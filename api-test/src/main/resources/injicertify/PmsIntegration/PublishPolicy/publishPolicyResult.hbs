{
    "response": {
        "policyGroupStatus": "{{policyGroupStatus}}",
        "is_Active": "{{is_Active}}",
        "policies": {
            "allowedAuthTypes": [
                {
                    "authType": "{{authType}}"
                }
            ]
        }
    }
}